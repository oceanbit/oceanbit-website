import {lightTheme, darkTheme} from '@oceanbit/styles';

export const theme = {
  light: lightTheme.colors,
  dark: darkTheme.colors
}

export const INITIAL_COLOR_MODE_CSS_PROP = "--initial-color-mode"
