---
import styles from "./waves.module.scss";
import WaveRight from "./wave-right.astro";

interface Props {
  className?: string | undefined;
  delay?: number;
}

const { className = "", delay = 0 } = Astro.props;
---

<WaveRight className={`${styles.flip} ${className}`} delay={delay} />
