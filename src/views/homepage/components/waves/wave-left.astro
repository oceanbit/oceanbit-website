---
import styles from "./waves.module.scss";
import WaveRight from "./wave-right.astro";

interface Props {
  className?: string | undefined;
  pathClass?: string | undefined;
}

const { pathClass = "", className = "" } = Astro.props;
---

<WaveRight className={`${styles.flip} ${className}`} pathClass={pathClass} />
